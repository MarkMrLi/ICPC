#include<bits/stdc++.h>

using namespace std;
typedef long long ll;
template
const int N = 2e3;
int prime[N];
bool is_prime[N];

void Prime() {
    prime[0] = 2;
    is_prime[2] = true;
    for()
}

void solve() {
    int n;
    cin >> n;
    vector<int> a(n);
    for(int i = 0; i < n; i++) cin >> a[i];
}

int main()
{
    int t;
    cin >> t;
    Prime();
    while(t--) solve();
    return 0;
}